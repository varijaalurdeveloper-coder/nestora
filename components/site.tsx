'use client'
import { useState } from 'react'
import Link from 'next/link'
import { ArrowRight, Check, Menu, MessageCircle, Phone, ShieldCheck, X } from 'lucide-react'
import type { Property, Location, Article } from '@/lib/site-data'

export function Navbar(){const [open,setOpen]=useState(false);const links=[['Properties','/properties'],['Locations','/locations'],['Services','/services'],['About','/about'],['Insights','/blog']];return <nav className="fixed inset-x-0 top-0 z-50 border-b border-white/15 bg-primary/95 text-primary-foreground backdrop-blur-md"><div className="mx-auto flex h-20 max-w-7xl items-center justify-between px-5 lg:px-8"><Link href="/" className="flex items-center gap-3"><span className="flex size-9 items-center justify-center rounded-full border border-accent text-accent">N</span><span className="text-sm font-bold tracking-[0.2em]">NESTORA <span className="text-accent">REALTY</span></span></Link><div className="hidden items-center gap-7 lg:flex">{links.map(([label,href])=><Link key={href} href={href} className="nav-link">{label}</Link>)}</div><Link href="/contact" className="hidden rounded-md bg-accent px-5 py-3 text-xs font-bold text-accent-foreground hover:bg-accent/90 sm:block">Book a Site Visit</Link><button className="lg:hidden" onClick={()=>setOpen(!open)} aria-label="Toggle navigation">{open?<X/>:<Menu/>}</button></div>{open&&<div className="flex flex-col gap-5 border-t border-white/10 bg-primary px-5 py-6 lg:hidden">{links.map(([label,href])=><Link onClick={()=>setOpen(false)} key={href} href={href}>{label}</Link>)}<Link onClick={()=>setOpen(false)} href="/contact" className="text-accent">Book a Site Visit</Link></div>}</nav>}
export function Footer(){return <footer className="bg-primary text-primary-foreground"><div className="mx-auto grid max-w-7xl gap-10 px-5 py-14 lg:grid-cols-4 lg:px-8"><div><p className="text-sm font-bold tracking-[0.2em]">NESTORA <span className="text-accent">REALTY</span></p><p className="mt-4 max-w-xs text-sm leading-6 text-primary-foreground/60">Helping Bengaluru make better property decisions.</p></div><div><p className="eyebrow text-accent">Explore</p><div className="mt-4 flex flex-col gap-3 text-sm text-primary-foreground/70"><Link href="/properties">Properties</Link><Link href="/locations">Locations</Link><Link href="/services">Services</Link><Link href="/blog">Insights</Link></div></div><div><p className="eyebrow text-accent">Company</p><div className="mt-4 flex flex-col gap-3 text-sm text-primary-foreground/70"><Link href="/about">About us</Link><Link href="/contact">Contact</Link><Link href="/find-your-property">Find your property</Link></div></div><div><p className="eyebrow text-accent">Talk to us</p><div className="mt-4 flex flex-col gap-3 text-sm text-primary-foreground/70"><a href="tel:+918012345678">+91 80 1234 5678</a><a href="mailto:hello@nestora.realestate">hello@nestora.realestate</a><span>Bengaluru, Karnataka</span></div></div></div><div className="border-t border-white/10 px-5 py-5 text-center text-xs text-primary-foreground/45">© 2026 Nestora Realty. Fictional portfolio project.</div></footer>}
export function WhatsAppButton({message='Hi, I\'m interested in exploring properties in Bengaluru.'}:{message?:string}){return <a href={`https://wa.me/918012345678?text=${encodeURIComponent(message)}`} target="_blank" rel="noreferrer" className="inline-flex items-center gap-2 text-sm font-semibold text-accent"><MessageCircle size={17}/> WhatsApp a Property Expert</a>}
export function LeadForm({compact=false}:{compact?:boolean}){const [state,setState]=useState<'idle'|'success'>('idle');if(state==='success')return <div className="flex min-h-72 flex-col items-center justify-center gap-4 text-center"><div className="flex size-12 items-center justify-center rounded-full bg-accent text-accent-foreground"><Check/></div><h3 className="font-serif text-2xl text-primary">We’ll be in touch shortly.</h3><p className="max-w-xs text-sm leading-6 text-muted-foreground">A Nestora property expert will contact you with a considered shortlist.</p><button onClick={()=>setState('idle')} className="text-sm font-semibold text-accent underline">Send another enquiry</button></div>;return <form onSubmit={e=>{e.preventDefault();setState('success')}} className="flex flex-col gap-4"><div className="grid gap-4 sm:grid-cols-2"><label className="field">Full name<input required placeholder="Your full name"/></label><label className="field">Phone number<input required type="tel" placeholder="+91 98765 43210"/></label>{!compact&&<label className="field">Email<input type="email" placeholder="you@example.com"/></label>}<label className="field">Preferred location<input placeholder="Whitefield, Hebbal..."/></label><label className="field">Property type<select defaultValue=""><option value="" disabled>Select type</option><option>Apartment</option><option>Villa</option><option>Plot</option><option>Investment property</option></select></label>{!compact&&<label className="field">Budget<input placeholder="e.g. ₹1.2 Cr"/></label>}</div>{!compact&&<label className="field">Message<textarea rows={4} placeholder="Tell us what you’re looking for"/></label>}<button className="flex h-12 items-center justify-center gap-2 rounded-md bg-accent px-5 text-sm font-semibold text-accent-foreground hover:bg-accent/90">{compact?'Get Property Recommendations':'Request Property Recommendations'} <ArrowRight size={16}/></button><p className="text-center text-xs leading-5 text-muted-foreground">Free consultation. No obligation. Your information is kept confidential.</p></form>}
export function PageHero({eyebrow,title,copy}:{eyebrow:string;title:string;copy:string}){return <section className="bg-primary px-5 pb-16 pt-36 text-primary-foreground lg:px-8"><div className="mx-auto max-w-7xl"><p className="eyebrow text-accent">{eyebrow}</p><h1 className="mt-5 max-w-4xl font-serif text-5xl leading-tight sm:text-7xl">{title}</h1><p className="mt-6 max-w-2xl text-base leading-7 text-primary-foreground/70">{copy}</p></div></section>}
export function PropertyCard({property}:{property:Property}){return <article className="group overflow-hidden rounded-lg border border-border bg-card"><div className="relative aspect-[4/3] overflow-hidden"><img src={property.image} alt={`${property.name} property`} className="size-full object-cover transition duration-500 group-hover:scale-105"/><span className="absolute left-4 top-4 flex items-center gap-1 rounded-full bg-primary px-3 py-1.5 text-[10px] font-bold uppercase tracking-wider text-primary-foreground"><ShieldCheck size={12} className="text-accent"/> Verified</span></div><div className="flex flex-col gap-4 p-5"><div><h3 className="font-serif text-2xl text-primary">{property.name}</h3><p className="mt-1 text-sm text-muted-foreground">{property.location}</p></div><div className="flex justify-between border-y border-border py-3 text-xs text-muted-foreground"><span>{property.bedrooms} · {property.area}</span><span>{property.type}</span></div><div className="flex items-center justify-between"><span className="font-serif text-xl text-primary">{property.price}</span><Link href={`/properties/${property.slug}`} className="flex items-center gap-1 text-xs font-bold text-accent">View property <ArrowRight size={14}/></Link></div><Link href={`/properties/${property.slug}#enquire`} className="flex h-10 items-center justify-center rounded-md border border-primary text-xs font-bold text-primary hover:bg-primary hover:text-primary-foreground">Request details</Link></div></article>}
export function LocationCard({location}:{location:Location}){return <Link href={`/locations/${location.slug}`} className="group relative min-h-72 overflow-hidden rounded-lg"><img src={location.image} alt={`${location.name} neighbourhood`} className="absolute inset-0 size-full object-cover transition duration-500 group-hover:scale-105"/><div className="absolute inset-0 bg-gradient-to-t from-primary via-primary/30 to-transparent"/><div className="relative flex min-h-72 flex-col justify-end p-6 text-primary-foreground"><h3 className="font-serif text-2xl">{location.name}</h3><p className="mt-2 max-w-xs text-sm leading-5 text-primary-foreground/75">{location.description}</p><span className="mt-4 flex items-center gap-2 text-xs font-bold text-accent">Explore properties <ArrowRight size={14}/></span></div></Link>}
export function ArticleCard({article}:{article:Article}){return <Link href={`/blog/${article.slug}`} className="group overflow-hidden rounded-lg border border-border bg-card"><div className="aspect-[16/10] overflow-hidden"><img src={article.image} alt="" className="size-full object-cover transition duration-500 group-hover:scale-105"/></div><div className="p-5"><p className="eyebrow text-accent">{article.category}</p><h3 className="mt-3 font-serif text-2xl text-primary">{article.title}</h3><p className="mt-3 text-sm leading-6 text-muted-foreground">{article.excerpt}</p><p className="mt-5 text-xs text-muted-foreground">{article.date}</p></div></Link>}
export function CTASection(){return <section className="bg-accent text-accent-foreground"><div className="mx-auto flex max-w-7xl flex-col gap-7 px-5 py-16 sm:flex-row sm:items-center sm:justify-between lg:px-8"><div><h2 className="font-serif text-4xl leading-tight sm:text-5xl">Your next home could be closer than you think.</h2><p className="mt-4 text-sm text-accent-foreground/75">Let’s find a property that fits your budget, lifestyle and future plans.</p></div><div className="flex flex-col gap-3"><Link href="/contact" className="flex h-12 items-center justify-center gap-2 rounded-md bg-primary px-6 text-sm font-bold text-primary-foreground">Book a Free Consultation <ArrowRight size={16}/></Link><WhatsAppButton/></div></div></section>}
