import { notFound } from 'next/navigation'
import { Navbar, Footer, CTASection } from '@/components/site'
import { articles, getArticle } from '@/lib/site-data'
export function generateStaticParams(){return articles.map(a=>({slug:a.slug}))}
export default async function ArticlePage({params}:{params:Promise<{slug:string}>}){const a=getArticle((await params).slug);if(!a)notFound();return <><Navbar/><main><section className="bg-primary px-5 pb-16 pt-36 text-primary-foreground lg:px-8"><div className="mx-auto max-w-4xl"><p className="eyebrow text-accent">{a.category} · {a.date}</p><h1 className="mt-5 font-serif text-5xl leading-tight sm:text-7xl">{a.title}</h1><p className="mt-6 max-w-2xl text-lg leading-8 text-primary-foreground/70">{a.excerpt}</p></div></section><article className="mx-auto max-w-3xl px-5 py-16 lg:py-24"><img src={a.image} alt="" className="aspect-[16/9] w-full rounded-lg object-cover"/><div className="mt-12 text-lg leading-8 text-muted-foreground"><p>Buying property in Bengaluru is rarely about one number. It is about the fit between your daily life, your financial comfort and the direction the neighbourhood is heading.</p><p className="mt-6">Start with the fundamentals: commute, schools, healthcare, community quality and the kind of flexibility you may need later. Compare a smaller number of options carefully instead of browsing endlessly.</p><h2 className="mt-12 font-serif text-3xl text-primary">A considered shortlist beats an endless search</h2><p className="mt-6">The right local context makes trade-offs visible. Speak with an expert, visit at different times of day and ask for the documents that support every important claim.</p></div></article><CTASection/></main><Footer/></>}
